---
import { BASE, SITE_TITLE } from '../consts';
import HeaderLink from './HeaderLink.astro';
---

<header>
	<nav>
		<span class="site-title"><a href={`${BASE}/`}>{SITE_TITLE}</a></span>

		<div class="internal-links">
			<HeaderLink href={`${BASE}/`}>Home</HeaderLink>
			<HeaderLink href={`${BASE}/blog`}>Articles</HeaderLink>
			<HeaderLink href={`${BASE}/projects`}>Projects</HeaderLink>
			<HeaderLink href={`${BASE}/about`}>About</HeaderLink>
		</div>

		<div class="social-links">
			<a href="https://github.com/gilbertossoares" target="_blank" rel="noopener noreferrer">GitHub</a>
			<a href="https://www.linkedin.com/in/gilbertossoares" target="_blank" rel="noopener noreferrer">LinkedIn</a>
		</div>
	</nav>
</header>

<style>
	header {
		margin: 0;
		padding: 0 1em;
		background: white;
		box-shadow: 0 2px 8px rgba(var(--black), 5%);
	}

	.site-title {
		margin: 0;
		font-size: 1em;
	}

	.site-title a,
	.site-title a.active {
		text-decoration: none;
	}

	nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
	}

	nav a {
		padding: 1em 0.5em;
		color: rgb(var(--black));
		border-bottom: 4px solid transparent;
		text-decoration: none;
	}

	nav a.active {
		text-decoration: none;
		border-bottom-color: var(--accent);
	}

	.internal-links,
	.social-links {
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	.social-links a {
		font-size: 0.95rem;
	}

	@media (max-width: 720px) {
		.social-links {
			display: none;
		}

		nav {
			flex-wrap: wrap;
		}

		.internal-links {
			width: 100%;
			justify-content: center;
		}
	}
</style>